# The main pipeline for weather_data_pipeline

resources:
  pipelines:
    weather_data_pipeline_etl:
      name: weather_data_pipeline_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/weather_data_pipeline_etl"

      libraries:
        - glob:
            include: ../src/weather_data_pipeline_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
